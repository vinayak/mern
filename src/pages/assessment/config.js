import React, { Component } from 'react';

class Config extends Component {
  render() {
    return (
      <div>

        shuffling questions yes no<br/>
        shuffling options yes no<br/>
        show marks/points for test<br/>
        allow user to move back and forward<br/>
        allow review mark in test<br/>
        question wise time<br/>
      show the result after completion<br/>
      message after completion<br/>
      define a pass percentage %<br/>
    show report after completion


      </div>
    );
  }
}

export default Config;
